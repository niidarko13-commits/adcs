# adcs

CubeSat ADCS Simulation

This repository contains a high-fidelity 3U CubeSat Attitude Determination and Control System (ADCS) simulation.

## 🚀 How to Run

> **Note:** The main simulation file is named `CubeSatADCSSimulation_FIXED.java` for compatibility reasons, but it is written in **Python**.

1. **Install Dependencies:**
   ```bash
   pip install -r requirements.txt
